 Running the application
  Once the key is set, you can run the agent in several ways:

  1. Local (using adk):

   1 adk web --port 8000

  2. Docker (using docker-compose):
   1 docker-compose up --build

  3. Evaluation Script:
   1 python eval/run_eval.py
